using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class loseStateManager : MonoBehaviour
{
    //when player rejects Tina in Yeemeow
    public static bool losestateYeemeow = false;

    //when player nwrites too many incoorect passwords/codes in xploit
    public static bool loasestaetXploit = false;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
